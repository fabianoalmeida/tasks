<p>
  <b>List Name:</b>
  <%= @list.name %>
</p>

<br />

<%= render :partial => 'tasks/index' %>

<% if can? :update, @list %><%= link_to 'Edit', edit_list_path(@list), :class => 'btn primary' %><% end %>
<%= link_to 'Back', lists_path, :class => 'btn' %>
